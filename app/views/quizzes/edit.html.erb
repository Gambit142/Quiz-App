<main class="quiz-container display">
  <h1>Editing quiz</h1>

  <%= render "form", quiz: @quiz %>

  <br>

  <div class="display">
    <%= link_to "Show this quiz", @quiz, class: 'new-quiz' %> |
    <%= link_to "Back to quizzes", quizzes_path, class: 'new-quiz' %>
  </div>
</main>